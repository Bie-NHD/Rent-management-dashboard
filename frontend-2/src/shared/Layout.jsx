import { Container } from "@mui/material";
import SideBar from "./SideBar";

const Layout = () => {
  return (
    <Container>
      <SideBar />
      <Container></Container>
    </Container>
  );
};

export default Layout;
