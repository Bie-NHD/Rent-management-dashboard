/**
 * @param {string} arr
 * @returns {string}
 */
export const formatDate = (arr) => `${arr[2]}/${arr[1]}/${arr[0]}`;
/**
 * @param {string} arr
 * @returns {string}
 */
export const formatId = (arr) => `${arr.split("-")[0]}...`;
