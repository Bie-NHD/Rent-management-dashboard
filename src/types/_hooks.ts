import { createMutation } from "react-query-kit";

//   const  create = <TData = TApiUpdateDTO | unknown>(variables:TData) => createMutation<Promise<TApiResponse   <TApiResponseData>>>({
//         mutationFn: async (variables) => Api.create(ApartmentURLs.Add, variables),
//     })
